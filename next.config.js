/** @type {import('next').NextConfig} */
const nextConfig = {
  // GLB files in /public/assets/ are served statically by Next.js without extra config
  // just ensure the public folder path is used
};

module.exports = nextConfig;
